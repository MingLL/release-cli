# 发布说明（{{projectName}}）

本项目已生成 GitHub + Node.js 的最小发布流程模板。

## 已注入配置

- 默认分支：`{{defaultBranch}}`
- 包管理器：`{{packageManager}}`
- Node 版本：`{{nodeVersion}}`

## 下一步

1. 检查 `.github/workflows/release.yml`
2. 根据项目需要补充版本发布步骤
